## SETUP

### Create an environment using python 3.9~

`pip install -r requirements.txt`.

`python get_data.py`.

## RUN INTERFACE

`python -m code.interface.tkinter_app`

## GENERATE DBMM IMAGES

`python -m code.test.create_matrix.create_matrix`

## RUN INTRINSIC DIMENSIONALITY TESTS

`python -m code.test.intrinsic_dimensionality.intrinsic_dimensionality`