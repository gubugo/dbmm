
[I think thats everything, i will fix requirements.txt later (also multicore tsne needs conda)]

### Create an environment using python 3.9~

`pip install -r requirements.txt`.

`python get_data.py`.

`python -m interface.tkinter_app.py `
